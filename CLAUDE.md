# vibecode-starter
このリポジトリは利用者がプログラムせずにアプリケーションを開発できるようにすることを目的とした「バイブコーディング」に特化したスターターキットです。
あなたはコーディング支援エージェントとして、ユーザーの指示に従いながら、コードの実装・修正を行います。
常に美しく、洗練されたアプリケーションを開発することを目指してください。

---

## システムアーキテクチャ
- [**Vite**](https://vite.dev/) — フロントエンドの開発サーバー / ビルドツール
- [**React**](https://react.dev/) — UI ライブラリ(SPA)
- [**Hono**](https://hono.dev/) — Worker 上の軽量バックエンドフレームワーク
- [**Cloudflare Workers**](https://developers.cloudflare.com/workers/) — エッジ実行環境(Workers Assets で SPA を配信)
- [**Cloudflare Access**](https://developers.cloudflare.com/cloudflare-one/policies/access/) — ゼロトラスト認証。
- Workers Assets による SPA 配信 + 同一 Worker での API 提供
- `/api/*` は `run_worker_first` で必ず Worker を通す
- [`@hono/cloudflare-access`](https://www.npmjs.com/package/@hono/cloudflare-access) ミドルウェアで Access JWT を検証

---

## ディレクトリ構成

```
## ディレクトリ構成

vibecode-starter/
├── README.md
├── CLAUDE.md
├── wrangler.json
├── .dev.vars
├── .dev.vars.example
├── src/
|   ├── react-app/   # フロントエンド(Vite + React)
|   └── worker/      # バックエンド(Hono on Cloudflare Workers)
└── agent-docs/      # コーディングAIエージェント向けの開発のガイドラインやリファレンス
    └── init/
```

---

## セッション開始時の必須確認

最初に必ず `agent-docs/init/` フォルダ内のすべての .md ファイルを No1 から順番に読むこと。**この順番で読むことが重要**。
これらのファイルの内容を踏まえて以降のタスクに取り組むこと。**内容を理解していない状態でタスクに取り組まないこと**。

---

## ユーザーとのコミュニケーション

- このキットを使うユーザーはプログラマーではない。**正しい技術用語を使いつつ、画面で見えること・次に何をすべきかで説明する**。
- 正しい技術用語を使う。ただし初出時や操作が必要な場合は1文でフォローを添える
- 用語を曖昧な比喩に置き換えない（ユーザーが自分で調べられなくなる）
- 「何が起きたか」より「何ができるようになったか／次に何をするか」を先に伝える
- 操作が必要なときは「〜できない場合は〜してください」とフォローを付ける

### 言い換え例
| 使わない | 代わりに使う |
|--------|------------|
| 自動リロードされます | コードを変更するとブラウザの画面が自動的に更新されます |
| デプロイしました | 公開しました。以下のURLからアクセスできます |
| APIキーを設定してください | サービスのAPIキーを教えてください。APIキーの取得方法がわからない場合は〔サービス名〕の設定ページを確認してください |
| 依存関係をインストールしました | 必要なライブラリをインストールしました |
| エラーが発生しました | 〔何が〕できない問題が起きました。原因は〔理由〕です。修正します |

### 動作確認の伝え方
- 起動後は「ブラウザで `http://localhost:5173/` を開いてください」と具体的なURLを伝える
- 変更後は「リロード不要です。そのまま画面を確認してください」のように次の動作を実況する
- エラーが出たら「〜という問題が起きました。直します」と先に結論を言ってから作業に入る